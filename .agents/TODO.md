# 待确认与后续验证

- [ ] 用户执行 `npm run build`；本仓库没有已配置的自动化测试、Lint 或格式化命令。
- [ ] 用户在已配置环境中验证登录、培训中心菜单、练习生成/复核与考试管理的实际接口响应、权限和异常提示。
- [ ] 在后端或部署任务开始前，确认对应服务的路由、请求/响应字段、数据库迁移、权限规则及目标环境；当前前端代码不是这些事实的证明。
- [ ] 若涉及运行时代理，确认外部如何安全注入 `docker.env` / `docker.local.env` 中的变量；不要在文档、命令历史或提交中记录变量值。
- [ ] `v-auth` 在多个页面中使用，但本次仅从前端调用点确认，未确认其注册及权限语义；修改按钮权限前先定位实现。
- [ ] 确认用户端/App/H5 的独立跳转目标与是否应在 Web 用户端中提供入口；当前 Web 端已安全禁用该入口。
- [ ] 用户执行 `npm run dev` 后验证 `/dashboard` 在管理端/Web 端状态下分别渲染正确首页，刷新后恢复当前端；并验证 `/web/home`、`/web/study`、`/web/practice`、`/web/exam` 的兼容访问、菜单高亮、管理端跳转以及 SPA 刷新。
- [ ] 用户验证管理端端类型栏与 Web 用户端之间多次双向切换、浏览器前进/后退和 Console；用户端/App/H5 的独立跳转目标仍待确认。
- [ ] 用户在真实环境以登录 token 联调已接入的用户端练习列表：验证 401、403、分类筛选、关键词、分页、空数据、网络失败，以及进入/返回 `ChatExam` 的完整链路。
- [x] 已接入用户端考试列表、状态统计和安全详情接口；开始/继续、结果与历史仍未实现。
- [x] 已接入正式考试开始、恢复、单题保存、交卷、评分结果和客户端超时尽力交卷；history、补考、人工评分和服务端自动交卷仍未实现。
- [ ] 用户在已登录目标环境验证 `/web/exam` 的列表、counts、分类、关键词、状态、分页、空态、错误重试、401/403/404/500 及三语题型显示。
- [ ] 用户确认后端目标库已部署用户考试记录表、Exam 的 LLM 评分环境变量及真实快照题型口径，并验证交卷、结果、失败重试和答案展示开关。
- [ ] 用户在部署后验证 ChatExam：`/v1/exams/start` 成功、`/v1/exams/answer` 的请求体为顶层 `session_id/messages`、422/500 显示错误而非空白气泡、200 SSE 正常流式显示。
- [ ] 用户验证结束考试的重新发布：修改为未来开始时间后生成新试卷版本，旧用户考试/成绩不变；进行中考试重复发布显示冲突。
- [ ] 用户验证 Web 用户端考试可用按钮悬停时没有黑色空 tooltip，禁用的开始/结果/历史按钮仍显示正确提示。

不把历史原型中的 localStorage、模拟数据或未确认功能列为实现待办。
# 2026-08-05 智能陪练待验证

- [ ] 由用户在可用 Smart Practice 环境验证自动首题、刷新恢复、初始化失败重试及结构化 SSE 的独立结果/下一题气泡。

- [ ] 用户执行 `npm run dev` 后验证 `/web/home`、`/web/study`、`/web/practice`、`/web/exam` 的访问、菜单高亮、管理端跳转以及 SPA 刷新。
- [ ] 确认用户端练习列表接口的授权范围、请求字段与响应契约；接入时由 adapter 映射为页面 `PracticeViewModel`，并验证 Web 练习进入与退出 `ChatExam` 的完整链路。
- [ ] 构建并部署 `beat-backend-github/src/comps/web_user_home/Dockerfile` 对应的用户端首页服务，注入 MySQL、账户鉴权和 `WEB_USER_HOME_SERVICE_*` 配置；前端生产 Nginx 同时注入 `WEB_USER_HOME_API_HOST`。
- [ ] 用户分别以 superadmin 与普通用户验证 `/web/home`：练习仅显示当前租户内已完成生成且有效的内容，考试只显示已发布且符合目标人群的内容。
- [ ] 如需要首页展示最近学习、今日计划、学习时长、练习进度或正确率，先设计并落地用户学习/练习行为记录表及写入流程，再扩展用户端首页接口。
## PPT/PPTX preview validation

- [ ] After deploying the updated Learn backend, verify that material-library and Web-study PPT/PPTX previews render as PDFs, while PDF, Word, Excel, image, and video previews keep their existing behavior.

## 2026-08-11 智能陪练答题 UI 验证

- [ ] 用户在桌面端和手机端验证 ChatExam 的 AI 教练消息与答题结果卡：普通消息宽度、结果卡紧凑程度、短答案/解析的自适应高度，以及长文本不产生横向滚动。

## 2026-08-11 本地用户端 iframe 验证

- [ ] 用户在 Vite 开发环境刷新后进入“用户端”，确认 iframe 加载 `http://localhost:5173/eap/index.html#/`，不再请求远程 H5 地址。

## 2026-08-11 用户端 SOP 练习跳转验证

- [ ] 用户在已登录环境从单个 SOP 的“开始练习”和“混合出题”确认进入 `/web/chat/exam`，确认 URL 查询参数、首题加载和返回行为正常。
- [ ] 用户在已登录目标环境验证综合练习：资料权限与题量不足错误、填空 15~30 / 问答 5~15 校验、启动后首题加载、SSE 答题、进度、结束和返回练习页；同时确认单项练习不受影响。

## 2026-08-11 端类型高亮验证

- [ ] 用户从 Web 端切换到用户端，确认仅用户端高亮；再切回 Web 端及管理端，确认三种端类型高亮状态正确。

## 2026-08-11 智能陪练移动端 UI 验证

- [ ] 用户在 360px、390px 和 430px 宽度验证：结果卡顶部三行信息、短答案/长解析的自然换行、下一题提示框对齐、输入区不遮挡消息，且无横向滚动。

## 2026-08-11 SOP 练习移动端分页验证

- [ ] 用户在 360px、390px 和 430px 宽度验证 SOP 列表的上一页/页码/下一页、跳页、总条数及底部提示均不重叠，且底部导航不会遮挡最后一项内容。

## 2026-08-11 资料预览渲染验证

- [ ] 用户验证 PDF、DOC/DOCX、XLS/XLSX 与 PPT/PPTX 首次打开时页面保持正常直立、无渲染动画；并确认 Ctrl 滚轮缩放及放大后的拖动仍可用。

## 2026-08-19 练习题目复核响应式验证

- [ ] 用户在练习题目复核页分别验证宽内容区、双栏中等内容区、侧栏展开后的中等内容区和窄内容区：宽屏保持原文件/题目复核双栏；双栏中的 DOCX 应缩放至左栏完整显示；空间不足时自动上下排列；题目搜索、分页、保存与返回操作仍可用。
# 2026-08-26 复核页 DOCX 预览待验收

- 在窄屏复核页打开含表格、图片和长段落的 DOCX：确认文字换行与原文一致，右侧内容可通过底部横向滚动查看，图片不被压缩或裁剪。
- 同时确认普通 DOCX、PDF、XLSX 预览以及“新窗口打开”“下载原文件”不受影响；构建和浏览器验证由用户执行。
# 2026-08-26 复核页 DOCX 整页缩放待验收

- 在 1024px、1280px 和宽屏环境打开含图片、表格和长段落的 DOCX：确认无横向滚动条、页面左右完整可见、文字没有额外换行、图片没有裁剪。
- 调整浏览器窗口宽度后确认缩放会更新；确认 PDF、XLSX、文本预览、下载和新窗口打开不受影响。构建和浏览器验证由用户执行。
